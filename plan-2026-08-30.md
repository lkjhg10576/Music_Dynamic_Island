# MDI 优化与开发计划（2026-08-30）

> 本文档冻结 2026-08-30 评审讨论的全部结论。执行时以本文档为准；任何范围变更先改本文档再动工。

## 一、背景

对项目做了全面评审（前端 ~11k 行 / 后端 ~3.7k 行），确认以下事实：

- `WidgetIsland.vue` 4997 行单组件（39 处定时器），`LiveActive.vue` 2562 行，`MainPanel.vue` 2204 行
- 163 处 localStorage 直读直写散落 5 个前端文件，main/widget 双窗口靠 emit 手动同步
- 流量统计跑在前端 `MainPanel.vue:960-979`，主窗口关闭/省内存销毁/静默自启时统计断档
- 3 份孤儿 Rust 副本（Cargo 不编译）：`src-tauri/lib.rs`（659 行）、`src/lib.rs`、`src/music_controller.rs`
- `WidgetIsland.vue` 多处中文注释乱码（1818/1823/1841/1846/2060/2934/3224/3264/3942/3962 行附近）
- 死代码：5 个前端从未调用的 command + `HW_EMIT_ENABLED` 只写不读
- `rust.yml` CI 缺 npm 步骤（`frontendDist: ../dist` 不存在）、上传 GB 级 target 产物、cargo test 空跑
- 全屏检测线程（`lib.rs:621-703`）消费方仅「全屏自动隐藏」一个功能

## 二、明确不做（已决策，勿复活）

| 项 | 原因 |
|---|---|
| 应用内自动更新 | 国内无法保证直连 GitHub Releases，用户自行下载更稳 |
| i18n 国际化 | 用户群中文为主，暂缓 |
| 配置导入/导出 | 设置项不多，意义不大 |
| 日志与错误上报 | 用户量小，无诡异 bug 报告压力 |
| 全屏检测改事件驱动 | 线程已做降频优化（句柄缓存+空闲 2s），SetWinEventHook 需补挂高频事件覆盖 Alt+Enter 场景，性价比低，**线程原样保留** |

## 三、执行清单（按顺序）

```
① 清理批次（小）→ ⑦ CI 修复（小）→ ② 流量统计下沉（中）→ ③ 设置单一数据源（大）
→ ⑤ 事件驱动改造（中，1.5~2 天）→ ④ 拆组件 + 下拉导航（大）
→ 6a 歌词自动缓存（中）→ 6b 歌词管理界面（中，1.5~2.5 天）
```

依赖规则：②在③前（②的落盘是③基建的第一个客户）；④在③后（先有数据源再拆组件）；6a 在⑤后（切歌链路理顺后挂缓存）、在③后（复用落盘基建）；6b 与④同批（独立组件 + 导航一起做）。

---

## ① 清理批次（小）

- 删除 3 份孤儿副本：`src-tauri/lib.rs`、`src/lib.rs`、`src/music_controller.rs`
- 修复 `WidgetIsland.vue` 乱码注释；新增 `.editorconfig` 统一 UTF-8
- 删除死代码：command `get_network_stats`、`get_hardware_stats`、`get_audio_spectrum`、`get_network_latency`、`fetch_notifications`；`HW_EMIT_ENABLED` 原子量与 `set_hardware_emit`（前端 `LiveActive.vue:811` 调用点同步删）
- README 漂移修正：「五大模块」→ 六模块（含 printer + sysmsg）；「打印机队列预留接口」→ 已完整实现

## ⑦ CI 修复（小）

- `rust.yml`：补 `npm ci --legacy-peer-deps` + `npm run build`（否则 `../dist` 不存在）；产物只上传 `src-tauri/target/release/bundle/`；`cargo test` 无测试则移除或保留但不再 `continue-on-error` 装样子
- 三个 workflow 与 `build.yml` 去重

## ② 流量统计下沉 Rust（中）

- 现状问题：`MainPanel.vue:960-979` 前端差值累计，`lastRx/lastTx` 为组件内变量，窗口重建即丢段；主窗口关闭后统计中断
- 方案：硬件监控线程（`lib.rs:317`，已有 1s 采样）内按天累计，落盘 `app_data_dir()`；前端改为只读快照
- 收益：省内存模式 / 静默自启下统计不再断档

## ③ 设置单一数据源（大）

- 现状：163 处 localStorage（WidgetIsland 53 / LiveActive 44 / DynamicSet 33 / MainPanel 17 / PersonalizeCenter 16），双窗口手动 emit 同步，`WidgetIsland.vue:717` 需「启动兜底读取」双保险
- 方案：Rust 侧 `config.json` 单一数据源 + `config-changed` 事件广播；`storageKeys.ts` 97 个 key 分批迁移，前端加统一存取层，不再直读 localStorage
- 落盘基建（目录管理、原子写）为 6a/6b 共用

## ⑤ 事件驱动改造（中，1.5~2 天）

**范围：只动音乐元数据 3s 轮询和歌词 50ms 扫描；进度 1s 轮询保持不动**（现有「1s 校准 + 本地时钟平滑推进」是正确设计；SMTC `TimelinePropertiesChanged` 播放中不推进，纯事件驱动进度条会冻结）。

### Rust 侧（大头）

- 新增会话绑定管理器（~150-250 行）：`CURRENT_SESSION: Mutex<Option<Session>>` 长期持有绑定会话（SMTC 事件挂在 Session 对象上，释放即停发，这是必须引入全局状态的根本原因）
- 挂三类事件并存 `EventRegistrationToken` 用于解绑：
  - `Session.MediaPropertiesChanged` → 提取歌名/歌手/播放态/来源，emit `music-info-changed`
  - `Session.PlaybackInfoChanged` → emit `music-playback-changed`（轻量，只更播放态）
  - `Manager.SessionsChanged` → 播放器退出/新启动时 rebind
- 语义对齐现状：当前会话变 Paused → rebind 到「正在播放」的会话（复刻现在 3s 轮询的自动切应用行为）
- 重入安全：回调内取快照 → 释放锁 → emit → 必要时 rebind，锁内不做 WinRT 调用
- 保留 `fetch_netease_music_info`（启动快照 + 兜底轮询用）；`control_system_media` / `get_music_timeline` / `seek_music` 不动

### 前端侧

- `syncMusicStatus` 的 1868-2000 行抽成 `applyMusicInfo(song, artist, playing, appId)`，事件直接调它
- `music-playback-changed` 只更新 `isPlaying` + 自动隐藏调度 + 进度时钟启停
- 删 3s `musicTimer`（3931 行）；启动时 invoke 快照一次
- **保留 30~60s 低频兜底轮询**（浏览器/视频类来源 SMTC 事件经常延迟或不发，兜底比对快照纠偏，不可省）

### 歌词调度

- `lyricMatcherTimer`（50ms 扫描，2376 行）→ 按 LRC 时间戳 `setTimeout` 精确调度，暂停即清除，校准仍走 1s 轮询

### 风险点

会话生命周期与 token 解绑；rebind 语义；浏览器源事件不可靠（靠兜底）。手测回归：网易云/QQ音乐/Spotify 切歌暂停退出重开；SMTC 多应用自动切换；浏览器视频标题变化；PotPlayer。

## ④ 拆组件 + 下拉导航（大）

- `WidgetIsland.vue` 按职责拆 composable（音乐同步 / 尺寸动画 / 实时活动 / 通知 / 歌词）
- MainPanel 导航：四视图轮换（`toggleDynamicSet`，384-393 行）→ 下拉列表直接跳转，复用现有 `metricDropdown` 模式；触发器文字改为显示当前页
- 视图注册第 5 项「歌词配置」→ `<LyricsManager>`（6b 交付）

## 6a 歌词自动缓存落盘（中）

- `fetch_netease_lyrics` 命中即落盘；切歌时先查本地，命中直接用
- **key = 规范化(歌名) + 规范化(歌手) + 时长(±2s 容差)**；key 生成收敛为 Rust 侧唯一函数（播放匹配 / 导入建条目 / 保存绑定三处共用，这是「保存后必命中」的根基）
- 存储：`lyrics/{hash}.lrc`（一律 UTF-8）+ `index.json`（key、文件名、来源 auto/user、时间）；放 `app_data_dir()`

## 6b 歌词管理界面（中，1.5~2.5 天）

独立组件 `LyricsManager.vue`，挂控制台第 5 页。

### 一级界面（缓存列表）

- 两列：歌名、歌手；行尾删除按钮
- 双击行 → 进入该条二级界面（搜索框自动预填）
- 「导入当前歌曲」按钮：调用 `import_current_track`（后端聚合 command，一次返回 SMTC 歌名/歌手/时长/来源）→ 建配置上下文进二级。**不开放手动建条目**（手填与 SMTC 上报可能不一致导致配完不命中）；未检测到播放时提示「请先播放歌曲」
- 只有保存成功才写索引，一级列表永远只显示「确实有词」的条目

### 二级界面（单首配置）

```
[歌名输入框] [歌手输入框] [搜索]   ← 两框+键总宽 = 下方两区总宽，左右对齐
┌─────────────────────────┬─────────────────────┐
│ 歌名│歌手│专辑│时长│来源  │ 歌词预览（原文，含    │
│ （QQ 10 + 网易 10 条）   │ [ti:] 时间轴，等宽    │
│                         │ 字体，只读）          │
│                         │ [导入]       [保存]  │
└─────────────────────────┴─────────────────────┘
```

- 无渠道选择、无搜索形式选择：固定聚合双源，QQ 5→10 条、网易 8→10 条，来源列标注
- 双击结果行 → 拉完整 LRC → 右侧预览**原文**（时间轴全保留，便于判断质量）
- 「导入」按钮（保存左侧、同尺寸）：`<input type="file">` 零依赖方案（不引 tauri-plugin-dialog），选本地 .lrc → 解码校验 → 填入预览区 → 走同一保存流程
- 确认弹窗**只判歌名**：候选歌名与当前条目歌名（规范化）不一致才弹；歌手不判（翻唱绑原唱词是合法操作）
- 未保存退出弹窗：「尚未保存配置，是否保存？」三键——**保存并退出**（主题色强调）/ 不保存 / 返回。拦截范围：组件内返回一级 + MainPanel 下拉切页（dirty 状态需 emit 给 MainPanel，switchView 时检查）；直接关窗不拦视为丢弃。dirty 判定：预览区内容与进入时已存歌词不一致（纯浏览不脏）
- 样式适配控制台现有风格（圆角卡片、明暗双主题）

### 导入 LRC 的 0 信任防线

1. 文件大小上限 1MB
2. 编码识别（详见下节）
3. 控制字符过滤（挡 NUL/转义序列）
4. 时间轴校验：至少命中若干 `[mm:ss.xx]` 行才算有效 LRC
5. **入库即归一 UTF-8**：猜编码只发生在导入这一刻，之后播放链路只读 UTF-8

### 编码识别方案（冻结）

```
字节流 → 1. TextDecoder('utf-8', {fatal:true}) 严格模式（利用 UTF-8 多字节结构特征，
            首字节 110/1110xxxx + 续字节 10xxxxxx；ASCII/UTF-8 在此结束）
       → 2. 失败进四赛道并行解码 + 特征打分：
            GBK（简体特征字：爱听见时…；天然覆盖 GB2312/GB18030）
            BIG5（繁体特征字：愛聽見時…）
            Shift-JIS（假名占比 U+3040-30FF，日文）
            EUC-KR（谚文占比 U+AC00-D7AF，韩文）
       → 3. 置信门槛：胜者乱码率 <1% 且得分显著领先第二名，否则拒收，
            提示「无法识别文件编码，请转换为 UTF-8 后导入」
```

注：`U+4E2D` 是码点书面记法，文件里不存在；UTF-8 的真实特征是字节结构，fatal 解码即在校验它。

### 后端新增 command

- `import_current_track` → `{歌名, 歌手, 时长, 来源}`（SMTC 聚合，未播放返回空）
- `search_lyrics_candidates(歌名, 歌手)` → 双源各 10 条 `{来源, 歌名, 歌手, 专辑, 时长, id}`（复用现有搜索逻辑，去掉自动匹配）
- `get_lyrics_by_candidate(来源, id)` → 完整 LRC 原文
- `save_lyrics_binding(key, 内容)` / `list_lyrics_cache` / `delete_lyrics_entry`

## 四、关键设计原则（跨批次通用）

1. **key 生成唯一**：歌名+歌手+时长的 normalize + hash 只在 Rust 侧一份实现
2. **入库即归一**：所有落盘文本一律 UTF-8，编码猜测只在入口发生
3. **数据 0 信任**：入口防线宁拒收不硬猜
4. **进度轮询不可动**：1s 校准 + 本地时钟是正确架构，事件驱动不覆盖进度链路
5. **浏览器/视频源不信任事件**：任何事件驱动改造都保留低频轮询兜底
